import { normalize } from '@Utils';

export const COLORS = {
    PRIMARY: '#6b5aa7',
    PRIMARY_LIGHT: '#f2f0ff',
    DANGER: '#f54336',
    WHITE: '#ffffff',
    BLACK: '#000000',
    GRAY: '#cccccc',
}

export const FONTS = {
    SMALL: `${normalize(12)}`,
    MEDIUM: `${normalize(16)}`,
    LARGE: `${normalize(20)}`,
};
